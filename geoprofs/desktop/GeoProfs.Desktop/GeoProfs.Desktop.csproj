<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <UseWPF>true</UseWPF>
    <Nullable>enable</Nullable>
    <RootNamespace>GeoProfs.Desktop</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- Nothing extra needed for the hello-world demo: HttpClient and
         System.Text.Json ship with .NET. Add packages here as the real
         app grows (e.g. CommunityToolkit.Mvvm for cleaner ViewModels). -->
  </ItemGroup>

</Project>
